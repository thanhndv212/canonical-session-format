{
  "name": "canonical-session-format",
  "version": "0.1.0",
  "description": "Canonical Session Format (CSF) — lean JSONL format for AI coding sessions",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "capture:opencode": "tsx scripts/capture-opencode.ts",
    "migrate": "tsx scripts/migrate-traces.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@sanity-labs/secret-scan": "^1.0.0",
    "better-sqlite3": "^12.11.1"
  }
}
